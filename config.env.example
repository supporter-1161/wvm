# --- Шаблон файла конфигурации скрипта wg-setup.sh ---
# Этот файл НЕ используется напрямую скриптом.
# Он служит примером структуры и значений по умолчанию.
# Файл, который создает и использует скрипт, находится в /etc/wireguard/config.env

# Основные настройки
WG_MODE="split"                      # split | full (режим по умолчанию при установке)
WG_PORT="29717"
WG_NET="10.99.0.0/24"
WG_SERVER_IP="10.99.0.1"             # IP сервера в VPN-сети

# Домашняя сеть (обязательно для обоих режимов)
HOME_NET="192.168.0.0/24"            # Диапазон домашней сети
HOME_DNS_WG_IP="10.99.0.100"         # IP домашнего шлюза в VPN-сети

# Сеть клиентов
CLIENT_NET_START="10.99.0.2"         # Первый возможный IP клиента
CLIENT_NET_END="10.99.0.254"         # Последний возможный IP клиента

# VPS настройки
SERVER_PUBLIC_IP=""                  # Публичный IP VPS (заполняется при установке)
PUBLIC_INTERFACE=""                  # Публичный интерфейс (определяется/уточняется при установке)

# Пути (на целевой системе VPS)
WG_CONFIG_FILE="/etc/wireguard/wg0.conf"
WG_PRIVATE_KEY_FILE="/etc/wireguard/private.key"
WG_PUBLIC_KEY_FILE="/etc/wireguard/public.key"
CLIENTS_DIR="/etc/wireguard/clients"
BACKUP_DIR="/etc/wireguard/backup"
LOG_FILE="/etc/wireguard/logs/setup.log"

# Статус установки
SETUP_COMPLETED="false"              # Меняется на "true" после успешной первоначальной настройки

